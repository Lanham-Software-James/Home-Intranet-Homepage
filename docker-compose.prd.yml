services:
  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: home-intranet-homepage
    networks:
      - home-intranet-nginx
    environment:
      VIRTUAL_HOST: "homepage.homeintranet.local"
      VIRTUAL_PROTO: "http"
      VIRTUAL_PORT: 3000
      HOMEPAGE_VAR_PIHOLE_PASSWORD: ${HOMEPAGE_VAR_PIHOLE_API_TOKEN}
      HOMEPAGE_VAR_OMADA_USERNAME: ${HOMEPAGE_VAR_OMADA_USERNAME}
      HOMEPAGE_VAR_OMADA_PASSWORD: ${HOMEPAGE_VAR_OMADA_PASSWORD}
      HOMEPAGE_VAR_OMADA_SITE: ${HOMEPAGE_VAR_OMADA_SITE}
    dns:
      - 192.168.0.39
    volumes:
      - ./config:/app/config
      - /var/run/docker.sock:/var/run/docker.sock

networks:
  home-intranet-nginx:
    external: true
